export declare class RuleParser {
    /**
     * Parse the given string of rules or array of rules
     * to a standard array format.
     *
     * @param rules Rule to be parsed.
     */
    static parse(rules: string | any[]): any[];
}
